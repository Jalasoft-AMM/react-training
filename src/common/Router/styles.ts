import { makeStyles } from '@mui/styles';

export default makeStyles({
  active: {
    color: 'green',
    background: '#121212',
    borderRadius: '0.5rem',
    textDecoration: 'none'
  },
  noActive: {
    color: 'white',
    background: 'none',
    textDecoration: 'none'
  }
});
